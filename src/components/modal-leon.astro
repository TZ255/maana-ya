<!-- Modal -->
<div
  class="modal fade"
  id="exampleModal"
  tabindex="-1"
  aria-labelledby="exampleModalLabel"
  aria-hidden="true"
>
  <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
    <div class="modal-content promo-modal">
      <div class="modal-header border-bottom-secondary border">
        <h1 class="modal-title fs-6" id="exampleModalLabel">
          TSH. 2,OOO CHAPCHAP! 🔥🔥🔥
        </h1>
        <button
          type="button"
          class="btn-close"
          data-bs-dismiss="modal"
          aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <p class="promo-copy">
          Tunatoa Tsh. 2,000 BURE! Ukijisajili Leo na Tsh. 5,000 BURE!
          Ukidownload App Yetu.
        </p>
        <div class="promo-image-wrap">
          <img
            src="/imgs/leonbet.webp"
            alt="Tsh. 5,000 Bure | Install LeonBet App"
            class="promo-image"
            onclick="OpenAff('leonbet')"
          />
        </div>
      </div>
      <div class="modal-footer">
        <button
          type="button"
          id="betBtn"
          class="btn btn-success w-100 promo-cta"
          onclick="OpenAff('leonbet')"><b>CHUKUA OFA YAKO!</b></button
        >
      </div>
    </div>
  </div>
</div>

<style>
  .promo-modal {
    border-radius: 0.85rem;
    overflow: hidden;
  }
  .promo-modal .modal-header {
    border-bottom: 0;
    padding-bottom: 0.25rem;
  }
  .promo-modal .modal-body {
    padding-top: 0.75rem;
  }
  .promo-copy {
    margin: 0 0 0.75rem;
    text-align: left;
  }
  .promo-image-wrap {
    border-radius: 0.7rem;
    overflow: hidden;
    border: 1px solid rgba(var(--color-border), 0.6);
  }
  .promo-image {
    width: 100%;
    max-height: 50vh;
    object-fit: cover;
    display: block;
  }
  .promo-cta {
    padding: 0.75rem 1rem;
    font-weight: 600;
  }
  @media (max-width: 575px) {
    .promo-modal .modal-body {
      padding: 0.75rem 1rem 1rem;
    }
    .promo-image {
      max-height: 45vh;
    }
  }
</style>

<script is:inline>
  // Function to show the modal after 30 seconds
  function showDelayedModal() {
    var myModal = new bootstrap.Modal(document.getElementById("exampleModal"));
    myModal.show();
  }

  // Set a timeout to call the function after 20 seconds
  setTimeout(showDelayedModal, 15000);
</script>
